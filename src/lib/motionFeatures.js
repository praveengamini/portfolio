// Async feature bundle for <LazyMotion features={loadFeatures} strict>.
// domMax is needed for `layout` / `layoutId` (nav pill, filter pills) and drag (MoreSheet).
export { domMax as default } from 'motion/react';
